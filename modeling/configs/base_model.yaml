MODEL:
  PIXEL_MEAN: [ 123.675, 116.280, 103.530 ]
  PIXEL_STD: [ 58.395, 57.120, 57.375 ]
  BACKBONE:
    NAME: "D2SwinTransformer"
    DIVISIBILITY: 64
  SWIN:
    EMBED_DIM: 192
    DEPTHS: [ 2, 2, 18, 2 ]
    NUM_HEADS: [ 6, 12, 24, 48 ]
    WINDOW_SIZE: 12
    APE: False
    DROP_PATH_RATE: 0.3
    PATCH_NORM: True
    PRETRAIN_IMG_SIZE: 384
    WEIGHTS: "swin_large_patch4_window12_384_22k.pkl"
  ENCODER:
    ENCODER_NAME: "MaskDINOEncoder"
    DIM_FEEDFORWARD: 1024
    HIDDEN_DIM: 256
    COMMON_STRIDE: 4
    NHEADS: 8
    NUM_LAYERS: 6  # TRANSFORMER_ENC_LAYERS
    NORM: "GN"
    NUM_CLASSES: 10
#  DECODER:
#    DECODER_NAME: "MaskDINODecoder"
#    DIM_FEEDFORWARD: 2048
#    HIDDEN_DIM: 256
#    NUM_OBJECT_QUERIES: 300
#    NHEADS: 8
#    DROPOUT: 0.0
#    NUM_LAYERS: 9  # 9+1, 9 decoder layers, add one for the loss on learnable query

VERSION: 2
